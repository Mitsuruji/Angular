import { DirListHoverDirective } from './dir-list-hover.directive';

describe('DirListHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DirListHoverDirective();
    expect(directive).toBeTruthy();
  });
});
