import { IoServStatusPipe } from './io-serv-status.pipe';

describe('IoServStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new IoServStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
