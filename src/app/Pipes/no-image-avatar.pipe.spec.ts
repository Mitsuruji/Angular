import { NoImageAvatarPipe } from './no-image-avatar.pipe';

describe('NoImageAvatarPipe', () => {
  it('create an instance', () => {
    const pipe = new NoImageAvatarPipe();
    expect(pipe).toBeTruthy();
  });
});
